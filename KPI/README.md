# 🎯 KPI چیست؟ (تعریف حرفه‌ای، نه کتابی)
- KPI (Key Performance Indicator) یعنی:
یک عدد قابل اندازه‌گیری که مستقیم نشان می‌دهد
آیا بیزینس دارد به هدفش نزدیک می‌شود یا نه.
❌ KPI = هر عددی که بشود حساب کرد (غلط)
✅ KPI = عددی که تصمیم‌سازی می‌کند

🧠 مدل ذهنی انتخاب KPI (قانون طلایی )
قبل از انتخاب KPI، همیشه این ۳ سؤال را بپرس- :
1️⃣ هدف بیزینس چیست؟
رشد؟
درآمد؟
نگه‌داشت مشتری؟
کاهش هزینه؟
2️⃣ تصمیمی که باید گرفته شود چیست؟
تبلیغ را زیاد کنیم یا نه؟
قیمت را عوض کنیم؟
روی کدام کشور تمرکز کنیم؟
3️⃣ چه عددی بهترین نماینده این تصمیم است؟
نه زیاد
نه گمراه‌کننده
نه Vanity Metric

❌ Vanity Metrics (دام رایج)
این‌ها عددند، ولی KPI نیستند:
تعداد کل کاربران از اول تاریخ
تعداد کل سفارش‌ها بدون بازه زمانی
مجموع فروش بدون مقایسه
چرا بدند؟
چون همیشه بالا می‌روند و چیزی نمی‌گویند.

✅ KPIهای خوب چه ویژگی‌هایی دارند؟ (SMART)
یک KPI خوب باید:
Specific (مشخص)
Measurable (قابل اندازه‌گیری)
Actionable (قابل اقدام)
Relevant (مرتبط با هدف)
Time-bound (در بازه زمانی)

📊 KPIهای پایه‌ای در Data Analytics (با مثال SQL)
فرض کنیم بیزینس تو فروش آنلاین است.

1️⃣ Customer KPIs (مشتری)
🔹 New Customers
هدف: رشد بازار
SELECT COUNT(*) AS new_customers_last_month
FROM customers
WHERE signup_date >= CURRENT_DATE - INTERVAL '1 month';

📌 تصمیم: تبلیغ جواب داده یا نه؟

🔹 Customers by Country
هدف: تمرکز بازار
SELECT country, COUNT(*) AS customer_count
FROM customers
GROUP BY country
ORDER BY customer_count DESC;

📌 تصمیم: روی کدام کشور سرمایه‌گذاری کنیم؟

2️⃣ Revenue KPIs (درآمد)
🔹 Total Revenue (بازه‌دار)
SELECT SUM(amount) AS revenue_last_30_days
FROM orders
WHERE order_date >= CURRENT_DATE - INTERVAL '30 days';

📌 بدون بازه زمانی → KPI نیست

🔹 Revenue per Customer (خیلی مهم)
SELECT
    c.name,
    SUM(o.amount) AS total_spent
FROM customers c
LEFT JOIN orders o ON c.customer_id = o.customer_id
GROUP BY c.name;

📌 تصمیم: مشتری‌های باارزش چه کسانی هستند؟

3️⃣ Engagement KPIs (درگیری مشتری)
🔹 Active Customers
تعریف: مشتری‌هایی که در X روز اخیر خرید کرده‌اند
SELECT COUNT(DISTINCT customer_id) AS active_customers
FROM orders
WHERE order_date >= CURRENT_DATE - INTERVAL '90 days';

📌 تصمیم: کاربران خوابیده داریم یا نه؟

4️⃣ Retention / Churn (سطح بالاتر)
🔹 Returning Customers
SELECT customer_id
FROM orders
GROUP BY customer_id
HAVING COUNT(*) > 1;

📌 تصمیم: آیا مشتری‌ها برمی‌گردند؟

🧠 انتخاب KPI درست برای Dashboard (قانون 5تایی)
به‌عنوان لید تیم، من اجازه نمی‌دهم بیشتر از ۵ KPI اصلی روی داشبورد باشد.
نمونه Dashboard فروش:
New customers (last 30 days)
Active customers (last 90 days)
Total revenue (monthly)
Avg revenue per customer
Top 5 customers by revenue
بیشتر از این → ذهن مدیر شلوغ می‌شود.

🚨 اشتباهات مرگبار در KPI (خیلی مهم)
❌ KPI بدون بازه زمانی
❌ KPI بدون مقایسه (قبل / بعد)
❌ KPI بدون owner (چه کسی مسئول است؟)
❌ KPI بدون اقدام (اگر بد شد، چه کار می‌کنیم؟)
